# 해시 이용 합집합 알고리즘
## 원소들의 연결 여부 확인
# dict를 이용해 해시처럼 사용

tc = int(input())
for _ in range(tc):
    tctc = int(input())
    one = []
    two = []
    for _ in range(tctc):
        x, y = input().split(' ')

    # 일단 패스!